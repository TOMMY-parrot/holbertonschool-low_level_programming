#include "main.h"
 char *_strdup(char *str);
 int length 
 if (str == 0);
 return NULL;
 for (length = 0 ; length != '/0' ; length++){
	 char *duplicate = malloc (sizeof length + 1) * (sizeof(char));
	 for (i = 0 ; i < length +1 ; i++)
		 return *duplicate[i]= char i

